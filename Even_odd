#include<bits/stdc++.h>
using namespace std;

void even_odd(int n){
    if(n%2 == 0){
        cout<<"Number is Even";
    }
    
    else{
        cout<<"Number is Odd";
    }
}


int main(){
    
    int n;
    cout<<"Enter the number: ";
    cin>>n;
    
    even_odd(n);
    
    return 0;
}
